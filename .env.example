# ============================================================================
# Concourse CI - 环境变量配置示例
# ============================================================================
# 说明:
#   当前 docker-compose.yml 配置中所有环境变量都已内置
#   此文件提供可自定义的配置选项供参考
#
# 使用方法:
#   1. 复制此文件为 .env: cp .env.example .env
#   2. 修改 .env 中的值
#   3. 修改 docker-compose.yml 使用环境变量替换
# ============================================================================

# -------------------- 数据库配置 --------------------
# PostgreSQL 数据库名称
POSTGRES_DB=concourse

# PostgreSQL 用户名
POSTGRES_USER=concourse_user

# PostgreSQL 密码（生产环境请修改）
POSTGRES_PASSWORD=concourse_pass

# -------------------- Concourse 配置 --------------------
# Concourse 外部访问地址
CONCOURSE_EXTERNAL_URL=http://localhost:8080

# Concourse 默认用户 (格式: username:password)
CONCOURSE_ADD_LOCAL_USER=test:test

# Concourse 主团队用户
CONCOURSE_MAIN_TEAM_LOCAL_USER=test

# 集群名称
CONCOURSE_CLUSTER_NAME=tutorial

# -------------------- 高级配置 --------------------
# Worker Baggageclaim 驱动 (overlay, btrfs, naive)
CONCOURSE_WORKER_BAGGAGECLAIM_DRIVER=overlay

# Worker 运行时 (containerd, guardian)
CONCOURSE_WORKER_RUNTIME=containerd

# DNS 服务器
CONCOURSE_WORKER_CONTAINERD_DNS_SERVER=8.8.8.8

# Client Secret (生产环境请使用安全的 Base64 编码密钥)
CONCOURSE_CLIENT_SECRET=Y29uY291cnNlLXdlYgo=

# TSA Client Secret (生产环境请使用安全的 Base64 编码密钥)
CONCOURSE_TSA_CLIENT_SECRET=Y29uY291cnNlLXdvcmtlcgo=

# -------------------- 功能开关 --------------------
# 启用 Pipeline 实例
CONCOURSE_ENABLE_PIPELINE_INSTANCES=true

# 启用 Across Step
CONCOURSE_ENABLE_ACROSS_STEP=true

# 启用 Resource Causality
CONCOURSE_ENABLE_RESOURCE_CAUSALITY=true

# -------------------- 端口配置 --------------------
# Concourse Web UI 端口
CONCOURSE_WEB_PORT=8080
